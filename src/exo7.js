const notes = class notes {
    constructor(note, moyenne)
    this.el = document.body;
this.bord = [];
this.moyenne = (somme + this.note) / 2;

};
function computeNotes() {
    const notes = computeNotes.notes;
    if moyenne = (somme + this.note) / 2
    return true;
};